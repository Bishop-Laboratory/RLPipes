name: rlpipes2
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - flake8
  - pytest
  - samtools
  - coverage
  # The last version with cores working correctly in api
  # TODO: submit an issue about this...
  - snakemake-minimal
  # Boa is required for conda mambabuild
  - boa
  - graphviz
  - pygraphviz
  - python >=3.7.1
  - pandas <=1.2.0
  - awscli
  - pyfastx
  - pysam >=0.17.0
  - click
  - pysradb >=1.0.1
